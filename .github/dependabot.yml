version: 2
updates:
  # Enable version updates for pnpm
  - package-ecosystem: "npm"
    directory: "/"
    # Check for updates once a week
    schedule:
      interval: "weekly"
    # Allow up to 2 open pull requests for pnpm dependencies
    open-pull-requests-limit: 2

